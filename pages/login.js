import React from 'react';
import Layout from '../components/Layout/Layout'

const Login = props => {
    return (
        <div>
            <Layout>
                <h1>Login</h1>
            </Layout>
        </div>
    )
};

export default Login;
